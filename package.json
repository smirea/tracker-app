{
  "name": "tracker",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "lint": "oxlint --fix",
    "test": "bun test",
    "db:generate": "drizzle-kit generate",
    "db:studio": "drizzle-kit studio",
    "db:dev": "turso dev --db-file ./local.db --port 8080",
    "db:push": "drizzle-kit push --dialect turso --schema ./db/schema.ts --url http://127.0.0.1:8080"
  },
  "devDependencies": {
    "@libsql/client": "^0.17.0",
    "drizzle-kit": "^0.31.8",
    "drizzle-orm": "^0.45.1"
  },
  "dependencies": {
    "@types/bun": "^1.3.7",
    "@typescript/native-preview": "^7.0.0-dev.20260127.1",
    "lefthook": "^2.0.16",
    "oxlint": "^1.42.0",
    "typescript": "^5.9.3"
  }
}
